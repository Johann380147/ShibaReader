<Window x:Class="ShibaReader.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ShibaReader"
        xmlns:view="clr-namespace:ShibaReader.Views"
        mc:Ignorable="d"
        Title="ShibaReader" Height="750" Width="1000"
        Icon="ShibaReader.ico">
    <DockPanel LastChildFill="True">
        
        <Grid Height="23" DockPanel.Dock="Top" HorizontalAlignment="Stretch">
            <Menu VerticalAlignment="Stretch" VerticalContentAlignment="Center">
                <MenuItem Header="Import">
                    <MenuItem x:Name="JILImportBtn" Header="JIL File" Click="JILImportBtn_Click"></MenuItem>
                    <MenuItem x:Name="CALImportBtn" Header="Calendar File" Click="CALImportBtn_Click"></MenuItem>
                </MenuItem>
            </Menu>
        </Grid>
        <StatusBar DockPanel.Dock="Bottom" Padding="3">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="3*"/>
                            <ColumnDefinition Width="Auto"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>

            <StatusBarItem HorizontalAlignment="Left">
                <Label x:Name="hoverDetailText"></Label>
            </StatusBarItem>
            <StatusBarItem Grid.Column="2" HorizontalAlignment="Right">
                <StackPanel Orientation="Horizontal">
                    <Label Margin="0,0,5,0" VerticalAlignment="Center">Status:</Label>
                    <ProgressBar x:Name="progressBar" Width="140" Height="15" Margin="0,0,10,0"/>
                </StackPanel>
            </StatusBarItem>
        </StatusBar>
        <StackPanel CanVerticallyScroll="True">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Label VerticalAlignment="Center" FontSize="14">Search:</Label>
                <TextBox x:Name="SearchText" Width="300" Height="30" Padding="3" VerticalAlignment="Center" Margin="10,10,10,10" KeyUp="SearchText_KeyUp"></TextBox>
                <Button Width="30" Height="30" Margin="0,0,5,0" IsEnabled="False">▲</Button>
                <Button Width="30" Height="30" Margin="0,0,15,0" IsEnabled="False">▼</Button>
            </StackPanel>
            <Label x:Name="jobNameText" HorizontalAlignment="Center" FontSize="24" FontWeight="Bold" MinHeight="42">
                <TextBlock Text="{Binding JobName}"></TextBlock>
            </Label>
            <view:JobDetailed Margin="0,10,0,0"/>
        </StackPanel>
    </DockPanel>
    
</Window>
